export class EncryptService {
  constructor() {}

  public crypt(key: string) {
    console.log(key);
    return key;
  }

  public encryptString(key: string) {
    console.log(key);
    return key;
  }

  public encriptar(key: string) {
    console.log(key);
    return key;
  }
}
